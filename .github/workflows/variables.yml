name: Variables
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    env:
      USERNAME: ${{ vars.USERNAME }} # Variablesの参照
    steps:
      - run: echo "${USERNAME}"
      - run: echo "${USERNAME2}"
      - run: echo "${USERNAME}"
      - run: echo "${USERTAN}"
